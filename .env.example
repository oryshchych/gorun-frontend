# Database (Optional - only if frontend connects directly to database)
DATABASE_URL="postgresql://user:password@localhost:5432/events_platform"

# NextAuth Configuration
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here-generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (Optional - for Google login)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Application URLs
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Backend API URL (REQUIRED)
# For local development: http://localhost:3001/api
# For production: https://your-backend-api.com/api
NEXT_PUBLIC_API_URL="http://localhost:3001/api"

# Vercel Deployment Notes:
# - All NEXT_PUBLIC_* variables are exposed to the browser
# - Set these in Vercel Dashboard → Project Settings → Environment Variables
# - NEXTAUTH_URL is automatically set by Vercel in production
# - Ensure backend API allows CORS from your Vercel domain
